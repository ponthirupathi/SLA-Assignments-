//Create a function to calculate the area of a rectangle.

let base = 70;
let height = 50;

function check( base , height){
    let area = 0.5*base*height;
    return area;
}
console.log(check(base,height));